# API Schemas

> REST/gRPC contract definitions for BFF ↔ domain service communication.

## Purpose
This directory will contain versioned request/response schemas (Pydantic models,
Protobuf/Avro definitions, or OpenAPI specs) that define the API contracts between:
- `iccc-bff` → core domain services
- `case-portal-bff` → core domain services
- `field-api` → core domain services

## Status
**Stub** — schemas will be added as BFF services are implemented.
